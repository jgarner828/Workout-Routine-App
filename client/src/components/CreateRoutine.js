import React from "react";

import { StyledRoutine } from "./styles/CreateRoutine.styled";

export default function CreateRoutine(props) {

    return (
        <StyledRoutine>
            Inside the styled Create Routine component
        </StyledRoutine>
    )
}